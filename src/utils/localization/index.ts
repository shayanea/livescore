import LocalizedStrings from 'react-native-localization';

export const localStrings = new LocalizedStrings({
  'fa-IR': {},
  'en-US': {},
});
// ? Set the language manually
localStrings.setLanguage('fa-IR');
